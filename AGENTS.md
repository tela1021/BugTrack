# AGENTS.md — BugTrack

> Auto-generated by harness-bootstrap

## Project overview

This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

- **Stack:** Next.js (React, TypeScript)
- **Type:** nextjs

## Repository structure

```

- `app/`
- `components/`
- `lib/`
```

## Conventions

### Commands

```bash
# Run locally
npm run dev

# Build
npm run build

# Lint
npm run lint

```

### Code style

Follow existing patterns in the codebase.

### Environment

Uses `.env` at project root. Do not commit secrets.

## Verification gates

Before marking any task complete:

- [ ] Manual verification completed
- [ ] Linter passes (`npm run lint`)
- [ ] No debug code, console.log, or TODOs left behind
- [ ] Changes are scoped to the task (no unrelated refactors)

## Permissions

**Allowed:**
- Read and edit source files
- Run build/test/lint commands
- Create files within project

**Restricted (ask):**
- Installing new dependencies (`npm install`, `pip install`)
- Modifying CI/CD or Docker config
- Destructive operations (rm -rf, database drops)

**Not allowed:**
- Committing secrets or .env files
- Pushing to production branches without review

## Global harness

This project inherits from `/Users/oleg/Projects/HARNESS.md`.
Read that file for universal agent rules.
